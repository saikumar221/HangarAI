services:
  postgres:
    image: postgres:16-alpine
    environment:
      POSTGRES_DB: hangarai
      POSTGRES_USER: hangar
      POSTGRES_PASSWORD: hangar
    ports:
      - "5432:5432"
    volumes:
      - postgres_data:/var/lib/postgresql/data

volumes:
  postgres_data:
